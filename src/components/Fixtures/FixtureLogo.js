import React from 'react';

const FixtureLogo = ({data}) => {
    return (
        <div className="team-logo">
            <i className="fas fa-basketball-ball"/>
        </div>
    );
};

export default FixtureLogo;
